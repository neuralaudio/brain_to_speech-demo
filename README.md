# Mid-Depth Speech Representation Targets Maximize Invasive Brain-to-Speech Decoding – audio demo

Static GitHub Pages site: open `index.html`, or serve the directory as-is. All audio is under `audio/<participant>/` and referenced relatively.

Regenerate with `python scripts/build_audio_demo.py` from the project root; k values whose 10-seed dumps are incomplete are marked pending and filled in on rerun.
